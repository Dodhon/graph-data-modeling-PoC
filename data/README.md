# Data Directory

- `input/`: source `.txt` manuals used for ingestion.
- `raw/`: original PDFs (optional, for reference).
- `output/`: generated JSON artifacts (progress, final, schemas, temporal).

Outputs are generated files and can be deleted and re-created by rerunning ingestion.
